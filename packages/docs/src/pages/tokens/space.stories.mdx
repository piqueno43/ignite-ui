import { Meta } from '@storybook/addon-docs'
import { TokensGrid } from '../../components/TokensGrid.tsx'
import { space } from '@ignite-ui-web/tokens'

<Meta title='Tokens/Spaces' />

# Spaces

Essas são as variáveis de espaçamento do Design System. Elas são usadas para definir o espaçamento entre elementos.

<TokensGrid tokens={space} hasRemValue />
